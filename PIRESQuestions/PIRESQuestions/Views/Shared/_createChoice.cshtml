@model Choice

<div class="container">
    <form asp-action="CreateChoice" asp-controller="Choice" id="formCreateChoice">
        @* <input type="text" asp-for="@Model.QuestionId" id="inputQuestionId" /> *@
        <div class="col-md-3 mb-3">
            <label asp-for="@Model.Label" class="form-label">Réponse :</label>
            <input type="text" asp-for="@Model.Label" class="form-control" id="inputLabel" placeholder="Saisissez le choix...">
            <span asp-validation-for="@Model.Label" class="text-danger"></span>
        </div>
        @* <button id="addChoice" class="btn btn-info">Choix supplémentaire</button> *@
        <button class="btn btn-info">Envoyer</button>
    </form>
</div>


<script>
    // $(document).ready(function () {
    //     // Fonction pour ajouter un nouveau champ d'entrée avec le bouton "Annuler"
    //     function addInput() {
    //         var newInput = '<div class="col-md-3 mb-3">' +
    //             '<label class="form-label">Réponse :</label>' +
    //             '<input type="text" name="additionalChoice " class="form-control" placeholder="Saisissez le choix...">' +
    //             '<button type="button" class="btn btn-danger cancelChoice">Annuler</button>' +
    //             '</div>';
    //         $('#additionalChoices').append(newInput);
    //     }

    //     // Lier un gestionnaire d'événements au bouton "Ajouter un choix"
    //     $('button#addChoice').on('click', function (e) {
    //         e.preventDefault();
    //         addInput();
    //     });

    //     // Lier un gestionnaire d'événements au bouton "Annuler" pour supprimer le champ d'entrée correspondant
    //     $(document).on('click', '.cancelChoice', function () {
    //         $(this).closest('.col-md-3').remove();
    //     });
    // });

</script>


